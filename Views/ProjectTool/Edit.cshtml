@model ProjectTool

<div class="col-12 grid-margin stretch-card mb-5">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title text-center">Edit Project Tool Information Information</h4>
            <form asp-action="Edit" method="post" class="forms-sample">
                <input asp-for="Id" type="hidden" />
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label asp-for="ToolName" class="form-label"></label>
                            <input asp-for="ToolName" type="text" class="form-control">
                            <span asp-validation-for="ToolName" class="text-danger"></span>
                        </div>
                    </div>

                </div>

                <button type="submit" class="btn btn-primary">Save Changes</button>
            </form>
        </div>
    </div>
</div>
